# Dashboard สรุปการทำแบบทดสอบก่อนอบรม — Learning T-Map

หน้าเว็บสรุปผลแบบทดสอบก่อนอบรม โครงการพัฒนาบุคลากรด้านการสร้าง Learning T-Map ปักหมุดแหล่งเรียนรู้คู่ชุมชน

- ดึงข้อมูลสดจาก Google Sheets (ชีตการตอบกลับของ Google Forms) แบบเรียลไทม์
- รีเฟรชอัตโนมัติ ตั้งได้ 15 วินาที / 30 วินาที / 1 นาที / 5 นาที หรือปิด
- สรุป KPI, กราฟการกระจายคะแนน, ไทม์ไลน์การเข้าทำ, ผลรายข้อ, สรุปตามหน่วยงาน และตารางรายชื่อผู้เข้าร่วม
- ตั้งเฉลยเองได้ (ถ้าไม่ตั้ง ระบบจะวิเคราะห์เฉลยจากความสัมพันธ์ระหว่างตัวเลือกกับคะแนนรวม)
- ส่งออกผลเป็นไฟล์ CSV เปิดด้วย Excel ได้

ไฟล์เดียวจบที่ `index.html` ไม่ต้องติดตั้งอะไรเพิ่ม

## เปลี่ยนไปใช้ชีตอื่น
ต่อท้าย URL ด้วย `?sheet=<SHEET_ID>` หรือแก้ค่า `SHEET_ID` ในไฟล์ `index.html`
ชีตต้องตั้งค่าแชร์เป็น "ผู้ที่มีลิงก์" (ผู้อ่าน)

## เกมสุ่มรายชื่อ (`random.html`)
หน้าสุ่มรายชื่อผู้นำเสนองาน ดึงรายชื่อสดจากชีตเดียวกัน (ตัดชื่อซ้ำอัตโนมัติ)
- สุ่มครั้งละ 1/2/3/5 คน มีแอนิเมชันชื่อวิ่ง เสียงประกอบ และพลุกระดาษ
- ไม่สุ่มซ้ำคนเดิม (จำไว้ใน localStorage) กรองเฉพาะอำเภอได้ คลิกชื่อเพื่อพักไม่ให้ถูกสุ่ม
- โหมดเต็มจอสำหรับฉายโปรเจกเตอร์ กด Spacebar เพื่อสุ่ม กด F เต็มจอ

## หน้าจัดการ/ล็อกรายชื่อ (`admin-x7k2m.html`) — ไม่มีลิงก์จากหน้าไหนทั้งสิ้น
ติ๊กเลือกว่าใครเข้าร่วมการสุ่มบ้าง — เอาติ๊กออก = ล็อกไว้ ไม่ให้ถูกสุ่ม
- จัดกลุ่มตามอำเภอ ติ๊ก/ล็อกทั้งกลุ่มได้ในคลิกเดียว มีช่องค้นหา
- ปุ่ม "ล็อกคนที่ถูกสุ่มไปแล้ว" ดึงประวัติจากหน้าเกมมาล็อกให้อัตโนมัติ
- บันทึกอัตโนมัติใน localStorage และสร้างลิงก์ `random.html?lock=ชื่อ1~ชื่อ2` เพื่อยกการล็อกไปใช้บนเครื่องอื่นได้

## ข้อกำหนดสำคัญของหน้าสุ่ม (`random.html`)
หน้านี้ใช้ฉายขึ้นจอต่อหน้าผู้เข้าร่วม จึง **ต้องไม่มีร่องรอยใด ๆ ว่ามีการล็อกรายชื่อ**
- ไม่แสดงจำนวนหรือรายชื่อที่ล็อก ไม่มีสัญลักษณ์หรือข้อความเกี่ยวกับการล็อก
- รายชื่อบนจอแสดงเฉพาะคนที่อยู่ในกลุ่มสุ่มจริง และซ่อนไว้เป็นค่าเริ่มต้น
- พารามิเตอร์ `?lock=` ถูกลบออกจากแถบที่อยู่ทันทีที่เปิดหน้า
- โหมดเต็มจอ (ปุ่ม ⛶ หรือกด F) ซ่อนปุ่มและตัวเลือกทั้งหมด เหลือเฉพาะเวทีสุ่ม

การจัดการทั้งหมดอยู่ที่ `admin-x7k2m.html` เท่านั้น

## รหัสผ่านหน้าจัดการรายชื่อ
`admin-x7k2m.html` มีหน้าล็อกก่อนเข้าใช้งาน เก็บเป็นค่าแฮช SHA-256 ของรหัสผ่านในไฟล์ (ไม่ได้เก็บรหัสตรง ๆ)
จำการเข้าใช้งานไว้ 12 ชั่วโมงต่อเครื่องได้ กรอกผิดครบ 5 ครั้งจะถูกหน่วงเวลา 30 วินาที

**ข้อจำกัดที่ต้องรู้:** หน้าเว็บนี้เป็นไฟล์นิ่งบน GitHub Pages ไม่มีเซิร์ฟเวอร์ตรวจสอบรหัสผ่าน
จึงกันคนทั่วไปที่เปิดลิงก์เจอได้ แต่กันคนที่เปิดดูซอร์สโค้ดเป็นไม่ได้
ถ้าต้องการการป้องกันจริงจัง ต้องย้ายไปใช้ Cloudflare Pages + Access หรือ Apps Script Web App ที่ตรวจสอบฝั่งเซิร์ฟเวอร์

**หน้าจัดการถูกซ่อนโดยเจตนา:** ไม่มีปุ่มหรือลิงก์ไปหน้านี้จากหน้า Dashboard และหน้าสุ่ม
เข้าได้ทางเดียวคือพิมพ์ URL เองแล้วกรอกรหัสผ่าน ถ้าเปลี่ยนชื่อไฟล์ ให้เปลี่ยนที่ไฟล์นี้ไฟล์เดียว
